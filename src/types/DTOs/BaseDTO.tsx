export interface BaseDTO {
  id: string;
  mainId?: string;
  createdAt?: string;
  updatedAt?: string;
}
export interface BaseReadDTO {
  id: string;
  mainId: string;
  createdAt: string;
  updatedAt: string;
}
